# == Class: <%= metadata.name %>
#
# This module installs <%= metadata.name %>
#
# @author <%= metadata.author %>
#
# @example Declaring the class
#   include <%= metadata.name %>
#
# @param package_name The name of the package which will be installed.
# @param service_name The name of the service.
# @param version The version of the package to install.
#
class <%= metadata.name %> (
  $package_name = $::<%= metadata.name %>::params::package_name,
  $service_name = $::<%= metadata.name %>::params::service_name,
  $version      = $::<%= metadata.name %>::params::version,
) inherits ::<%= metadata.name %>::params {

  contain ::<%= metadata.name %>::install
  contain ::<%= metadata.name %>::config
  contain ::<%= metadata.name %>::service
  contain ::<%= metadata.name %>::checks

}
