# == Class <%= metadata.name %>::params
#
# This is a private class which is meant to be called from <%= metadata.name %>.
# It sets sane default variables.
#
class <%= metadata.name %>::params {

  $package_name = '<%= metadata.name %>'
  $service_name = '<%= metadata.name %>'
  $version      = 'present'

}
