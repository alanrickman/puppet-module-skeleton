# == Class <%= metadata.name %>::checks
#
# This is a private class meant to be called from <%= metadata.name %>
# This class installs Check MK client side checks for <%= metadata.name %>.
#
class <%= metadata.name %>::checks {

  require ::<%= metadata.name %>::install

}
